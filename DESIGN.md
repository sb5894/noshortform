# ShortsBlocker Android UI 디자인 검토

작성일: 2026-09-05

범위: Kotlin·Jetpack Compose UI 분석 및 개선 설계. 1–7절은 코드 수정 전 작성한 설계이며, 후속 구현 결과는 8절에 기록한다.

## 1. 현재 UI 구조

### 분석 근거와 범위

`MainActivity.kt`, `ui/theme/`의 세 파일, `strings.xml`, 설정 저장 클래스, 접근성 서비스·감지·차단 정책, Manifest 및 백업 설정을 확인했다. 현재 앱을 기기나 에뮬레이터에서 실행한 시각 검증은 하지 않았다. 아래 평가는 코드에 드러난 구조와 문구를 기준으로 하며, 첨부 이미지는 현재 앱의 실행 화면이 아니라 사용자의 UI 제안으로 취급한다.

프로젝트는 Kotlin 기반 네이티브 Android 앱이다. 이미 Jetpack Compose와 Material 3를 사용하므로 UI 기술을 전환할 필요가 없다. `minSdk`는 24이며 기존 Compose 의존성만으로 제안한 표현을 구현하는 방향이다. 프로젝트 리소스의 앱 이름은 `No-shortform`이고, 이 문서에서는 사용자가 부르는 ShortsBlocker를 사용한다. 앱·서비스 식별 이름 변경은 별도 작업으로 남긴다.

### 화면과 상태 연결

현재 별도 탐색 구조 없이 `MainActivity`에서 `NoshortformTheme → Scaffold → ObserverScreen`을 표시한다. 화면은 전체 높이를 사용하는 스크롤 가능한 `Column`이며 좌우·상하 24dp 여백, 주요 자식 사이 20dp 간격을 사용한다.

현재 배치 순서는 다음과 같다.

1. 제목 ‘YouTube 이벤트 관찰’과 개발용 프로토타입 설명
2. 접근성 서비스 켜짐·꺼짐 텍스트
3. ‘Shorts 차단’ 라벨과 Material 3 `Switch`
4. 차단 상태 및 개발 빌드 제한 설명
5. ‘차단 방식’ 제목과 세로 라디오 선택 행 두 개
6. 데이터 처리, 감지 근거, 사용법에 관한 긴 안내
7. ‘접근성 설정 열기’ 버튼

현재 모드는 `LEGACY`(스크롤 차단)와 `ENTRY`(진입 차단) 중 하나만 선택한다. 선택 행 전체에 `selectable`과 라디오 역할을 부여하고, 그룹에 `selectableGroup`을 적용한 구조다. 차단을 꺼 둔 상태에서도 디버그 빌드에서는 모드를 선택할 수 있다.

`MainActivity.onResume()`이 저장된 차단 설정과 해당 접근성 서비스 활성화 여부를 다시 읽는다. 차단과 모드 변경 콜백 및 컨트롤에는 `BuildConfig.DEBUG` 제한이 있으며, 서비스도 릴리스 빌드에서는 이벤트 처리를 수행하지 않는다. 디자인 변경으로 이 제한을 해제해서는 안 된다.

### 현재 테마

- `Color.kt`: 기본 보라·회보라·분홍 계열 토큰.
- `Theme.kt`: 시스템 다크 모드를 따르며, Android 12 이상에서 동적 색상을 기본 사용한다. 실제 색은 기기 배경화면에 따라 달라질 수 있다.
- `Type.kt`: `bodyLarge`만 16sp/24sp로 정의하며 나머지는 Material 기본 타이포그래피를 사용한다.
- 기존 화면에 전용 카드, 배경 장식, 주요 상태를 강조하는 시각적 영역은 없다.

## 2. 투박하게 느껴지는 구체적인 이유

| 관찰 | 사용자에게 미치는 영향 | 개선 방향 |
| --- | --- | --- |
| 개발용 제목과 설명이 화면 첫 부분을 차지함 | Shorts 차단이라는 사용 목적보다 개발 도구라는 인상이 먼저 전달됨 | 첫 화면은 조작과 현재 설정 중심으로 구성하고 기술 설명은 정보 영역으로 이동 |
| 다수의 텍스트와 조작 요소가 동일한 세로 흐름에 배치됨 | 상태 확인, 행동, 참고 설명의 우선순위를 구별하기 어려움 | 접근성 상태, 주 스위치, 차단 방식, 상세 안내의 네 단계로 정리 |
| 거의 모든 주요 요소 사이에 같은 20dp 간격을 사용함 | 관련된 설명끼리의 묶음과 섹션 경계가 약함 | 그룹 내부 4–12dp, 그룹 사이 24–32dp로 구분 |
| 접근성 설정 버튼이 긴 설명 뒤에 있음 | 최초 설정이나 서비스 꺼짐 상태에서 필요한 행동이 늦게 나타남 | 접근성 상태 바로 옆 또는 바로 아래로 이동 |
| 일반 텍스트만으로 서비스 상태를 표시함 | 스위치 상태와 실제 사용 전제 조건을 한눈에 구별하기 어려움 | 상태 라벨과 보조 설명을 함께 표시 |
| 기본 보라색 또는 기기 동적 색상을 사용함 | 원하는 연두색·평온한 분위기가 일관되게 유지되지 않음 | 고정된 연두·세이지 색상 체계 사용 |
| 모드 선택이 일반 설정 목록 형태임 | 두 방식의 차이와 현재 선택이 시각적으로 약함 | 설명을 포함한 선택형 표면과 라디오 표시 사용 |
| 첫 화면에 Logcat·shortsEvidence 설명까지 노출됨 | 개인용 도구의 단순한 사용 흐름에 비해 읽을 양이 많음 | 정확한 설명은 유지하면서 접을 수 있는 정보 영역으로 이동 |

기존 화면의 스크롤 지원, 표준 Switch·RadioButton, 라디오 그룹 의미 구조는 보존할 장점이다.

## 3. ShortsBlocker에 맞는 디자인 방향

### 방향: 햇빛이 비치는 연두빛 유리 표면

따뜻한 연백색 바탕에 넓고 흐린 연두색 원형 그라데이션을 배치한다. 그 위에 반투명한 밝은 표면을 얹어 가벼운 유리 느낌을 만든다. 짙은 녹색 글자와 조작 요소가 중심을 잡고, 배경 장식은 주의를 요구하지 않는다. 힐링은 복잡한 일러스트나 감성 문구보다 부드러운 색, 읽기 쉬운 정보량, 예측 가능한 조작으로 표현한다.

개인용 앱에 맞게 단일 화면을 유지한다. 계정, 통계, 스트릭, 타이머, 별도 온보딩, 하단 탭은 추가하지 않는다. 앱을 열면 서비스 상태와 차단 설정을 확인하고 닫을 수 있어야 한다.

### gpt-taste 원칙의 적용 범위

참고한 스킬: [gpt-taste/SKILL.md](.agents/skills/gpt-taste/SKILL.md).

시각적 위계, 명확한 버튼 대비, 절제된 카드 수, 목적 있는 여백, 은은한 배경 표현 원칙을 반영한다. 스킬의 웹 전용 규칙인 React, Tailwind, HTML/CSS, GSAP, AIDA 랜딩 페이지 구성, 스크롤 고정, 마우스 호버, 무작위 레이아웃·폰트 선정, 외부 이미지 로딩은 적용하지 않는다. 사용자 지시와 네이티브 앱의 목적을 우선한다. 정적인 배경을 허용하며 UI 설계에 무작위 선택이나 이미지 생성은 필요하지 않다.

### 첨부 시안 판단

시안의 정보 순서는 적절하며 대부분 채택한다. 특히 상단 접근성 상태와 설정 이동, 중심 차단 스위치, 두 모드, 하단 정보 링크는 현재 화면보다 목적이 분명하다.

| 시안 요소 | 반영 여부와 조정 |
| --- | --- |
| 상단 접근성 상태 + 설정 바로가기 | 채택. 좁은 화면에서는 버튼을 상태 아래로 내려 긴 한국어가 충돌하지 않게 함 |
| 중앙의 큰 차단 스위치 | 주요 조작이라는 강조는 채택. 스위치 자체를 확대·재제작하지 않고 넓은 강조 영역 안에 표준 Material 3 Switch 사용 |
| 스위치 주변 별 모양 | 생략. 배경 원형 그라데이션으로 부드러운 장식 역할을 대체 |
| 두 개의 가로 모드 카드 | 두 선택지 구조는 채택. 휴대폰은 세로 배치가 기본이며 충분히 넓은 화면에서만 가로 배치 검토 |
| 청록색 불투명 면과 진한 테두리 | 연두색 투명 표면과 얇은 경계로 변경. 선택된 요소에만 짙은 녹색 강조 |
| 큰 중간 공백 | 축소. 화면 높이의 비율로 고정하지 않고 콘텐츠에 맞춘 간격 사용 |
| 하단 ‘정보 및 사용법’ | 채택. 같은 화면에서 펼치는 TextButton으로 구현하여 별도 탐색 의존성 없이 안내 제공 |

연두색과 글래스모피즘은 잘 어울린다. 다만 배경이 글자 뒤로 강하게 비치면 평온함보다 읽기 불편함이 커진다. 따라서 실제 배경 흐림 효과보다 밝고 불투명도가 높은 표면으로 유리를 암시하는 방식을 권장한다.

## 4. 색상, 타이포그래피, 간격, 버튼, 카드 설계

### 색상 토큰 초안

다음 값은 구현 시작점이다. 투명 표면은 합성된 실제 배경과 함께 대비를 확인해야 하며, 아래 표 자체가 검증 완료된 대비 결과는 아니다.

| 역할 | 라이트 | 다크 | 사용 |
| --- | --- | --- | --- |
| 배경 | `#F5F8EF` | `#141B16` | 전체 화면 |
| 기본 표면 | `#FAFCF6` | `#202A22` | 정보와 조작 영역 |
| 주 강조색 | `#3F6444` | `#B8D9A8` | 주 버튼, 선택 표시, 스위치 |
| 강조색 위 글자 | `#FFFFFF` | `#18351F` | FilledButton 라벨 |
| 선택 컨테이너 | `#DDEDCB` | `#30452F` | 선택한 모드 배경 |
| 선택 컨테이너 글자 | `#233923` | `#DFEED6` | 선택한 모드 텍스트 |
| 기본 글자 | `#233127` | `#E5ECE1` | 제목과 주요 정보 |
| 보조 글자 | `#526050` | `#B5C1B0` | 설명과 보조 상태 |
| 경계 | `#899783` | `#73846E` | 필요한 구분선과 컨트롤 경계 |
| 설정 필요 표면 / 글자 | `#F2EAD4` / `#65512B` | `#3D3523` / `#E6D5A6` | 서비스 꺼짐 안내 |
| 배경 원형 색 | `#D5E8B7`, `#E5EFCB` | `#344C31`, `#293E30` | 비상호작용 장식 |

연두색은 넓은 배경과 선택 표면에서 느껴지게 하고, 흰 글자를 연한 연두 위에 올리지 않는다. 상태는 색만으로 전달하지 않고 ‘켜짐’, ‘꺼짐’, ‘선택됨’ 등 텍스트·라디오 표시를 함께 사용한다. 일반 본문은 대비 4.5:1 이상, 조작 식별에 필요한 경계와 상태 표시는 3:1 이상을 검증 목표로 삼는다.

브랜드 색을 유지하기 위해 향후 `NoshortformTheme`의 동적 색상 기본 사용을 끄는 방향을 제안한다. 시스템 다크 모드 대응은 유지하고, 다크에서도 짙은 회녹색 바탕과 은은한 연두색을 사용한다. 테마의 나머지 `onSurface`, `outline`, 비활성·오류 역할도 확인해 기존 보라색이 섞이지 않게 한다.

### 글래스모피즘의 구현 수준

- 루트 `Box` 뒤에 `Brush.radialGradient`를 사용하는 정적인 장식 원 2–3개를 배치한다. 상단 한쪽에 큰 원, 주 조작 영역 반대쪽에 작은 원 정도로 시작한다.
- 라이트 표면은 밝은 색의 약 88–94% 불투명도를 시작점으로 사용한다. 다크는 약 94–98%로 더 차분하게 한다. 가독성이 부족하면 불투명 표면으로 올린다.
- 표면 모서리는 20–24dp, 선택 모드는 16–20dp. 유리 테두리는 1dp 정도로 약하게 두되 선택 상태 식별은 별도 강조색·라디오가 담당한다.
- 배경 장식은 콘텐츠 레이아웃이나 터치 영역을 차지하지 않고 접근성 탐색에서도 제외한다.
- 실제 실시간 배경 블러, RenderEffect, 화면 캡처, 외부 블러 라이브러리는 사용하지 않는다. 장식이 필요하면 현재 Compose 그리기 기능으로 해결한다.
- 그림자는 없거나 1–2dp 수준으로 시작한다. 지속적으로 움직이는 원·광택·입자 효과는 넣지 않는다.

### 타이포그래피

시스템 기본 글꼴 `FontFamily.Default`를 사용한다. 다운로드 폰트나 영문 전용 서체를 추가하지 않는다. 한국어의 안정적인 폴백과 사용자 글꼴 설정을 우선한다.

| 용도 | 크기 / 줄 높이 | 굵기 |
| --- | --- | --- |
| 주 조작 제목 ‘Shorts 차단’ | 26sp / 34sp | SemiBold |
| 화면 제목·섹션 제목 | 20sp / 28sp | Medium |
| 모드 제목 | 16sp / 24sp | SemiBold |
| 본문·상태 설명 | 14–16sp / 22–24sp | Normal |
| 버튼 라벨 | 14sp / 20sp | Medium |

한국어 본문 자간은 0sp를 시작점으로 한다. 제목을 한두 줄로 읽기 쉽게 작성하되, 글꼴 확대 시 줄 수 제한·말줄임·강제 축소로 내용을 숨기지 않는다. 긴 안내는 14sp보다 작게 줄이지 않고 정보 영역으로 이동한다.

### 간격과 크기

- 휴대폰 좌우 여백 24dp, 매우 좁은 화면에서는 16dp까지 조정.
- 제목과 설명 사이 6–8dp, 상태와 관련 버튼 사이 12dp.
- 표면 내부 여백 20dp, 모드 표면 내부 여백 16dp.
- 모드 선택지 사이 12dp, 주요 영역 사이 24–32dp.
- 실제 터치 영역 최소 48×48dp. 버튼 높이는 최소 48dp로 두고 큰 글꼴에서는 늘어나게 한다.
- 주 조작 영역도 내용에 맞춰 높이가 늘어나게 하며 화면 높이 비율이나 고정 Spacer로 위치를 맞추지 않는다.
- `Scaffold`의 시스템 바 안쪽 여백과 기존 세로 스크롤을 유지한다. 태블릿에서는 콘텐츠 최대 너비 약 560dp로 중앙 정렬한다.

### 버튼과 카드

접근성 서비스가 꺼져 있으면 ‘접근성 설정 열기’를 FilledButton으로 강조한다. 켜져 있으면 동일한 기능을 OutlinedButton 또는 TextButton으로 낮춰 차단 조작에 시선이 모이게 한다. 서비스는 앱에서 자동으로 켜지지 않고 기존 시스템 설정 화면에서 사용자가 직접 활성화한다.

주 조작 영역에는 제목, 상태 문구, 표준 Material 3 `Switch`를 배치한다. 조작 영역 전체를 별도의 중복 클릭 대상으로 만들지 않는다. 스위치를 그래픽처럼 확대하거나 비표준 드래그 동작으로 대체하지 않는다.

모드 선택지는 각각 하나의 선택형 표면으로 만들고 제목, 기존 설명, RadioButton을 담는다. 두 카드는 독립적인 켜기·끄기 버튼이 아니다. 선택 상태는 연두색 컨테이너, 짙은 테두리, 라디오로 함께 전달한다. 선택지 전체가 한 번만 읽히고 한 번만 선택되도록 기존 `selectableGroup`·`Role.RadioButton`·자식 `onClick = null` 의미 구조를 보존한다.

큰 유리 표면 하나 안에 모드 카드와 정보 카드까지 중첩하지 않는다. 강조 영역 하나와 모드 선택 표면 두 개를 중심으로 충분하다. 모션은 Material 기본 스위치·리플을 우선하며, 별도 선택색 전환이 필요할 때만 150–200ms 수준으로 검토한다.

## 5. 화면별 개선안

현재 실제 화면은 하나뿐이다. 아래는 이 화면의 상태별 개선안과 새로 제안하는 인라인 정보 영역이며, 다중 화면 앱으로 확장하는 제안이 아니다.

### 5.1 기본 화면

위에서 아래로 다음 순서를 사용한다.

1. 작은 앱 제목 `No-shortform`.
2. ‘접근성 서비스: 켜짐/꺼짐’과 설정 이동 버튼.
3. ‘Shorts 차단’ 제목, 표준 스위치, 현재 설정을 설명하는 짧은 문장.
4. ‘차단 방식’ 아래 스크롤 차단·진입 차단 선택 표면.
5. ‘정보 및 사용법’ 펼치기 버튼과 펼쳐진 내용.

작은 기기에서 모든 내용을 한 화면에 억지로 넣지 않는다. 상단 상태와 차단 스위치를 우선하고 나머지는 자연스럽게 스크롤한다. 정보 버튼은 콘텐츠 뒤에 두며 고정 바나 오버레이로 본문을 가리지 않는다.

### 5.2 상태별 표시

| 조건 | 주 조작 영역 문구 예시 | 조작 |
| --- | --- | --- |
| 디버그, 서비스 꺼짐, 차단 꺼짐 | ‘차단이 꺼져 있어요. 사용하려면 접근성 서비스를 켜 주세요.’ | 스위치·모드 변경 가능, 설정 버튼 강조 |
| 디버그, 서비스 꺼짐, 차단 켜짐 | ‘차단 설정은 켜져 있어요. 사용하려면 접근성 서비스를 켜 주세요.’ | 저장된 스위치 켜짐 유지, 실제 작동 중으로 표시하지 않음 |
| 디버그, 서비스 켜짐, 차단 꺼짐 | ‘차단이 꺼져 있어요. 자동 뒤로 가기는 하지 않아요.’ | 스위치·모드 변경 가능 |
| 디버그, 서비스 켜짐, 차단 켜짐 | ‘차단 설정이 켜져 있어요. Shorts 감지 시 뒤로 가기를 요청해요.’ | 선택 모드 표시, 성공·완전 차단 보장 표현 금지 |
| 릴리스, 모든 저장 상태 | 기존 ‘관찰과 차단은 개발 빌드에서만 사용할 수 있습니다. 이 빌드에서는 동작하지 않습니다.’ | 기존처럼 스위치·모드 변경 비활성화, 저장값 자체는 임의 변경하지 않음 |

접근성 활성화 여부는 시스템 설정에서 확인한 상태이며 서비스의 현재 연결·감지 성공을 실시간으로 증명하지 않는다. ‘보호 중’, ‘완벽 차단’, 차단 횟수처럼 현재 코드가 제공하지 않는 상태를 표시하지 않는다.

접근성 꺼짐이나 차단 꺼짐을 이유로 모드 선택까지 비활성화하지 않는다. 사용자가 모드를 먼저 고를 수 있는 현재 동작을 유지한다. 릴리스에서만 기존 제한을 따른다.

### 5.3 모드 설명

첨부 시안의 ‘기존 설명 그대로’를 반영하여 다음 문구를 유지한다.

- **스크롤 차단:** 다음 영상으로 넘길 때 차단됩니다. 첫 영상 허용을 보장하지는 않습니다.
- **진입 차단:** Shorts에 들어갔을 때부터 차단을 시도합니다. 첫 화면이 잠깐 보일 수 있습니다.

모드 이름과 `LEGACY`/`ENTRY`의 매핑을 그대로 둔다. ‘첫 영상은 항상 허용’, ‘Shorts가 아예 열리지 않음’ 같은 의미로 설명을 바꾸지 않는다.

### 5.4 정보 및 사용법

`TextButton`을 누르면 같은 화면 아래에 안내가 펼쳐지고, 다시 누르면 접힌다. 펼침 상태는 필요하면 `rememberSaveable`로 유지하되 차단 설정 저장소와 분리한다. 키보드·TalkBack으로 접근할 수 있고 펼침·접힘 상태를 알 수 있게 한다.

정보는 다음 세 묶음으로 정리한다.

- **사용법:** 시스템 접근성 설정에서 ‘No-shortform 이벤트 관찰’을 직접 켜는 방법, 앱의 차단 스위치와 모드 선택, 서비스 끄는 방법.
- **데이터 처리:** 기존 Logcat 메타데이터 항목과 화면 전체 내용·영상 제목·이미지·로그 파일을 저장하거나 네트워크 전송하지 않는다는 안내.
- **동작 범위:** 개발 빌드 제한, `shortsEvidence`의 의미와 한계, 2초 연속 요청 제한, 첫 화면이 잠깐 나타날 가능성.

기존 안내의 사실과 한계를 보존하고 ‘아무 정보도 읽지 않는다’ 또는 ‘로그를 남기지 않는다’처럼 오해를 만드는 문구로 단순화하지 않는다. 개발 빌드 제한과 접근성 설정 필요 안내는 접힌 정보에만 숨기지 않고 해당 상태의 주 화면에도 표시한다.

### 5.5 시스템 접근성 설정 화면

Android가 제공하는 외부 시스템 화면이다. 디자인 변경 대상이 아니며 기존 `Settings.ACTION_ACCESSIBILITY_SETTINGS` 이동과 설정 앱을 열 수 없을 때의 Toast 대체 안내를 유지한다. 복귀하면 기존 `onResume()` 경로로 상태를 갱신한다.

## 6. 수정할 파일 목록

다음은 향후 구현 단계의 후보이며, 이번 작업에서 수정하는 파일은 `DESIGN.md`뿐이다.

| 파일 | 향후 변경 범위 |
| --- | --- |
| `app/src/main/java/com/example/no_shortform/MainActivity.kt` | `ObserverScreen`의 Compose 배치·표현 변경, 인라인 정보 영역 추가. Activity의 상태 읽기·저장 콜백·디버그 제한·설정 이동은 보존 |
| `app/src/main/java/com/example/no_shortform/ui/theme/Color.kt` | 연두·세이지 중심 라이트·다크 색상 토큰 |
| `app/src/main/java/com/example/no_shortform/ui/theme/Theme.kt` | Material 3 색상 역할 연결, 동적 색상 기본값 조정, 시스템 다크 모드 유지 |
| `app/src/main/java/com/example/no_shortform/ui/theme/Type.kt` | 제목·본문·버튼 글자 크기와 줄 높이 정리 |
| `app/src/main/res/values/strings.xml` | 화면용 제목·상태 문구·정보 펼치기 라벨 정리. 모드 설명, 서비스 식별 이름, 보안 안내의 사실은 유지 |

작은 프로젝트이므로 위 파일 범위에서 시작한다. UI가 길어져 분리가 필요할 때만 `ObserverScreen.kt`를 현재 패키지에 추가하여 기존 composable을 옮긴다. 디자인 전용 프레임워크, 탐색 라이브러리, 이미지 로더, 별도 상태 관리 계층은 추가하지 않는다. Gradle·Manifest·서비스 설정·런처 리소스 변경은 필요하지 않다.

변경 제외 파일:

- `YouTubeEventObserverService.kt`
- `ShortsEvidenceDetector.kt`
- `ShortsBlockingPolicy.kt`
- `BlockingSettings.kt`, `BlockingMode.kt`
- `AndroidManifest.xml`, `res/xml/accessibility_service_config.xml`
- `res/xml/backup_rules.xml`, `res/xml/data_extraction_rules.xml`

## 7. 기능을 깨뜨리지 않기 위한 주의사항

### 기능·보안 경계

1. `BuildConfig.DEBUG` 제한을 UI, 변경 콜백, 서비스 모두에서 유지한다. 개인용이라는 이유로 릴리스 차단을 활성화하지 않는다.
2. `blocking_settings`의 저장소 이름과 `enabled`, `mode` 키, 기본 차단 꺼짐, 기본·잘못된 값 대체 모드 `ENTRY`를 유지한다. UI 재구성이나 Preview가 설정을 기록하지 않게 한다.
3. 모드 전환은 기존 콜백으로만 저장하며 스위치 값을 함께 바꾸지 않는다. 차단을 끄거나 서비스가 꺼져 있어도 선택 모드는 유지한다.
4. 서비스 재시작이나 타이머 초기화를 UI에 추가하지 않는다. 현재 서비스는 이벤트마다 설정 스냅샷을 읽으며 모드 변경이 2초 제한을 초기화하지 않는다.
5. `com.google.android.youtube` 대상 제한과 정확한 두 뷰 ID 비교를 유지한다. 텍스트·contentDescription 읽기, 노드 트리 탐색, 스크린샷, 다른 앱 감시를 추가하지 않는다.
6. 1초보다 오래된 이벤트 제외, 미래·음수 시간 제외, 실패한 요청을 포함한 2초 연속 요청 제한, `GLOBAL_ACTION_BACK` 경로를 그대로 유지한다.
7. 인터넷 권한, Firebase, 광고, Analytics, 외부 폰트·이미지 다운로드, 이벤트 저장·전송을 추가하지 않는다. 기존 메타데이터 Logcat 범위와 설정 백업 제외 규칙을 보존한다.
8. 화면의 서비스 상태는 기존 시스템 조회 결과를 사용한다. 접근성을 자동 활성화하거나 저장된 차단 설정만으로 서비스 상태를 추정하지 않는다.
9. TalkBack에는 Switch 역할·체크 상태·비활성 상태와 모드의 단일 선택 의미가 전달되어야 한다. 장식에는 의미를 부여하지 않고 부모·자식 중복 클릭 및 중복 읽기를 피한다.
10. 터치 영역, 글꼴 확대, 스크롤, 시스템 바 여백을 보존한다. 상태 설명을 색이나 애니메이션만으로 전달하지 않는다.

### 향후 구현 후 검증 기준

| 영역 | 확인 내용 |
| --- | --- |
| 변경 범위 | 서비스·감지·정책·설정 저장·권한·의존성에 불필요한 변경이 없는지 diff 확인 |
| 기존 회귀 검사 | `testDebugUnitTest`, `assembleDebug`, `assembleRelease`, `lintDebug` 실행 및 새 오류 확인 |
| 설정 유지 | 최초 설치 기본값, 앱 재실행·회전, 차단 꺼짐 상태에서 모드 변경, 설정 화면 복귀 후 상태 갱신 |
| 기능 경로 | 디버그에서 두 모드와 차단 끄기 동작이 UI 변경 전과 동일한지 실제 기기 확인 |
| 릴리스 제한 | 스위치·모드 선택 비활성화와 제한 안내, 관찰·차단이 활성화되지 않음 확인 |
| 상태 조합 | 접근성 켜짐/꺼짐 × 차단 켜짐/꺼짐 네 조합에서 안내가 정확한지 확인 |
| 표시 | 라이트·다크, 320–360dp 폭, 가로 화면, 글꼴 200%에서 잘림 없이 스크롤·조작 가능 |
| 접근성 | TalkBack 탐색 순서, 스위치와 라디오 단일 읽기, 정보 펼침 상태, 48dp 터치 영역, 합성 배경 대비 |
| 성능 | 배경 정지 상태에서 지속 애니메이션·타이머가 없고 API 24에서도 블러 의존 없이 표시되는지 확인 |

이 문서 작성 단계에서는 앱 코드 변경이나 빌드·기기 테스트를 수행하지 않았다. 구현 시에는 위 기준으로 검증하며 기존 테스트 통과를 실제 기기 차단 성공의 증거로 대신하지 않는다.

## 8. 구현 결과

작업 브랜치: `feature/calm-native-ui`. 아래는 사용자의 후속 구현 요청에 따라 반영한 결과다.

### 파일별 변경 전후

| 파일 | 변경 전 | 변경 후 |
| --- | --- | --- |
| `MainActivity.kt` | Activity와 전체 화면 composable이 한 파일에 존재 | 화면을 `ObserverScreen.kt`로 분리. Activity 클래스의 기능 처리 본문은 원본과 동일 |
| `ObserverScreen.kt` (추가) | 별도 파일 없음 | 연두 원형 그라데이션, 반투명 주 조작 표면, 표준 Switch, 세로 라디오 선택 표면, 접는 정보 영역. 라이트·다크·큰 글꼴 Preview 포함 |
| `ui/theme/DesignTokens.kt` (추가) | 색상·타입 파일과 화면 내부에 값이 분산 | 라이트·다크 색상, 타이포그래피, 간격, 모서리, 투명도, 배경 장식 비율을 한 파일에서 관리 |
| `ui/theme/Color.kt`, `Type.kt` (삭제) | 기본 보라색과 기본 본문 정의 | `DesignTokens.kt`로 통합하여 중복 정의 제거 |
| `ui/theme/Theme.kt` | 배경화면 동적 색상과 기본 색상 사용 | 고정된 세이지 색상과 시스템 다크 모드 사용. 추가 표면 역할은 CompositionLocal로 공급 |
| `res/values/strings.xml` | 개발용 제목, 긴 상태 안내, 아래쪽 설정 버튼을 전제로 한 문구 | 상태 조합별 간결한 안내와 정보 펼치기 문구 추가. 대체된 미사용 문구 제거. 서비스 이름·설명, 모드 설명, 기존 데이터·감지 안내 보존 |
| `androidTest/.../ObserverScreenTest.kt` (추가) | 화면 동작 테스트 없음 | 차단 꺼짐 상태의 모드 변경, 서비스 없이 켜진 차단 설정의 안내와 설정 이동 콜백, 정보 펼침 상태 복원 검증 |
| `gradle/libs.versions.toml` | 테스트용 Espresso 3.5.1 | 테스트용 Espresso 3.7.0. 실행 앱의 의존성·권한은 추가하지 않음 |

실시간 블러 대신 정적인 radial gradient와 높은 불투명도의 Surface를 사용했다. 작은 화면에서 버튼을 상태 아래에 두고 모드를 세로로 배치한다. 주 조작 표면을 중복 클릭 대상으로 만들지 않았으며, 차단이 꺼져 있거나 접근성이 꺼져 있어도 기존처럼 디버그에서는 모드를 선택할 수 있다.

테스트 의존성 변경은 최초 에뮬레이터 테스트에서 `InputManager.getInstance`의 `NoSuchMethodException`이 발생했기 때문이다. [Android 공식 Espresso 3.7.0 변경 기록](https://developer.android.com/jetpack/androidx/releases/test#espresso-3.7.0)에 해당 호출 교체가 명시되어 있다. 변경 후 동일한 테스트가 통과했다.

### 검증 결과

- `testDebugUnitTest`: 24개 통과, 실패 0.
- `assembleDebug`, `assembleRelease`: 성공.
- `lintDebug`: 오류 0, 경고 15. 남은 경고는 기존 Manifest 라벨, 의존성 버전 및 미사용 리소스 항목이다.
- `connectedDebugAndroidTest`: Pixel 6a / Android 17 에뮬레이터에서 4개 통과. 신규 UI 테스트 3개와 기존 앱 컨텍스트 테스트 1개다.
- 라이트·다크 화면, 320dp 폭·200% 글꼴에서 상단 표시 및 모드 영역으로의 세로 스크롤을 캡처하여 확인했다. 검사 후 에뮬레이터 글꼴·크기·밀도 설정을 복구했다.
- 서비스, 감지기, 정책, 설정 저장, 모드 모델, Manifest, 접근성·백업 XML 및 `app/build.gradle.kts`의 변경 없음 확인. Activity 클래스 본문도 원본과 문자열 비교로 동일함을 확인했다.

로컬 시각 검증 이미지: `app/build/calm-ui-light.png`, `app/build/calm-ui-dark.png`, `app/build/calm-ui-large-text.png`, `app/build/calm-ui-large-text-scrolled.png`. 빌드 디렉터리 산출물이므로 clean 시 삭제될 수 있다.

이번 검증은 UI 조작·표시와 기존 단위 테스트 범위다. 실제 YouTube를 통한 차단 재검증, TalkBack 음성 탐색, API 24 기기 검증은 수행하지 않았다. 접근성 서비스를 자동으로 켜거나 사용자의 실제 기기에 앱을 설치하지 않았다.

### 2026-09-06 사용자 피드백 반영

- 접근성 상태 텍스트 왼쪽, 설정 열기 버튼 오른쪽의 Row로 수정했다. 두 영역에 너비를 배분하고 큰 글꼴에서는 각 영역 안에서 줄바꿈한다. 서비스 활성화 시 OutlinedButton으로 연하게 표시하는 동작은 유지한다.
- 차단 표면, 두 모드 표면, 펼친 안내 블록에 공통 `glassBlock` 표현을 적용했다. 밝은 대각선 반사, 점차 흐려지는 유리 테두리, 약한 그림자와 투명한 색 면을 사용한다. 투명도·반사·경계·그림자 값은 `DesignTokens.kt`에서 관리한다.
- `testDebugUnitTest` 24개 통과, 디버그·릴리스 빌드 성공, Lint 성공. `connectedDebugAndroidTest`는 연결된 Pixel 6a 에뮬레이터와 SM-S931N에서 각각 4개씩 통과했다. 이 후속 실행은 실제 연결 기기도 포함했다. YouTube 차단 검증이나 서비스 자동 활성화는 수행하지 않았다.
- 수정된 에뮬레이터 화면을 `app/build/calm-ui-glass.png`로 저장하여 오른쪽 버튼 배치와 반투명 표면을 확인했다.
